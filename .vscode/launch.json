{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Karaoke Party Server",
            "program": "${workspaceFolder}/server/index.js",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/server/.env",
            "args": [
                "--inspect"
            ],
            "runtimeExecutable": "node"
        }
    ]
}